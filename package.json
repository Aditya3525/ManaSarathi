{
  "name": "manasarthi",
  "version": "1.0.0",
  "description": "ManaSarathi - Full Stack Monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "setup:env": "node scripts/setup-local-env.cjs",
    "doctor:config": "node scripts/config-doctor.cjs",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"wait-on http://localhost:5000/api/health && npm run dev:frontend\"",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "npm run build --workspace frontend",
    "build:backend": "npm run build --workspace backend",
    "start": "npm run start:backend",
    "start:backend": "npm run start --workspace backend",
    "lint": "npm run lint:frontend && npm run lint:backend",
    "lint:frontend": "npm run lint --workspace frontend",
    "lint:backend": "npm run lint --workspace backend",
    "format": "npm run format:frontend && npm run format:backend",
    "format:frontend": "npm run format --workspace frontend",
    "format:backend": "npm run format --workspace backend",
    "typecheck": "npm run typecheck:frontend && npm run typecheck:backend",
    "typecheck:frontend": "npm run typecheck --workspace frontend",
    "typecheck:backend": "npm run typecheck --workspace backend",
    "test": "npm run test:backend && npm run test:frontend",
    "test:frontend": "npm run test --workspace frontend",
    "test:backend": "npm run test --workspace backend",
    "ci": "npm run lint && npm run typecheck && npm run build && npm run test",
    "clean": "rimraf frontend/dist frontend/node_modules backend/dist backend/node_modules node_modules",
    "reset": "npm run clean && npm install",
    "db:migrate": "npm run migrate --workspace backend",
    "db:studio": "npm run db:studio --workspace backend",
    "db:generate": "npm run db:generate --workspace backend",
    "setup": "npm install && npm run db:generate && npm run db:migrate",
    "setup:local": "npm install && npm run setup:env && npm run setup:auto --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "rimraf": "^5.0.5",
    "wait-on": "^7.2.0"
  },
  "engines": {
    "node": ">=18",
    "npm": ">=8"
  },
  "keywords": [
    "wellbeing",
    "ai",
    "react",
    "nodejs",
    "typescript",
    "monorepo",
    "manasarthi"
  ],
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Aditya3525/ManaSarathi.git"
  },
  "dependencies": {
    "docx": "^9.6.1"
  }
}
